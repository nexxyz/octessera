{
  "name": "@octessera/device-contracts",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "node -e \"process.exit(0)\"",
    "format:check": "node -e \"process.exit(0)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "test:coverage": "c8 --check-coverage --lines 80 --functions 80 --branches 70 --reporter=text --reporter=lcov tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "tsx": "^4.22.0",
    "typescript": "^5.9.3"
  }
}
