[package]
name = "octessera-desktop"
version = "0.7.5"
description = "octessera desktop shell"
authors = ["octessera"]
edition = "2021"
license-file.workspace = true

[lib]
name = "octessera_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
playback-runtime = { path = "../../../crates/playback-runtime" }
realtime-engine = { path = "../../../crates/realtime-engine" }
rodio-engine-source = { path = "../../../crates/rodio-engine-source" }
midir = "0.10"
rodio = "0.20"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.0.0", features = [] }
tauri-plugin-dialog = "2"

[dev-dependencies]
playback-runtime = { path = "../../../crates/playback-runtime", features = ["test-support"] }
