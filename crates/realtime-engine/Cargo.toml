[package]
name = "realtime-engine"
version = "0.8.0"
edition = "2021"
license-file.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
platform-capabilities-build = { path = "../platform-capabilities-build" }
