[package]
name = "rodio-engine-source"
version = "0.7.5"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
crossbeam-channel = "0.5"
realtime-engine = { path = "../realtime-engine" }
rodio = "0.20"
serde_json = "1"
